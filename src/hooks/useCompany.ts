'use client'

import { useCompany as useCompanyContext } from '@/contexts/CompanyContext'

/** Convenience re-export — use this in components instead of importing from contexts directly */
export { useCompanyContext as useCompany }
